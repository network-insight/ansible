---
- name: remove {{ package }}
  hosts: localhost
  tasks:
  - name: stop {{ package }}
    service:
      name: "{{ package }}"
      enabled: false
      state: stopped
  - name: uninstall {{ package }}
    package:
      name: "{{ package }}"
      state: absent
